# AI Finance App

Full-stack demo: backend (Express + lowdb) and frontend (React). See backend/ and frontend/ folders. Start backend: `cd backend && npm install && npm start`. Start frontend: `cd frontend && npm install && npm run dev`.

This is a demo starter — replace lowdb with a proper DB for production and secure JWT secret.
